type User {
  id: ID!
  name: String!
  email: String!
  balance: Float!
  createdAt: String!
}

type Mutation {
  createUser(name: String!, email: String!): User!
}

type Query {
  user(id: String!): User
}